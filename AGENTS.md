Agent Guidelines
----

- When working in a Git worktree, as a final step make sure `cargo clippy` and `cargo test` both pass.

Code Guidelines
----

- When importing modules or functions, DO NOT use `use` groups. Prefer using one `use` statement per line
